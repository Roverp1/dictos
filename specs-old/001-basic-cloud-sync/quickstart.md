# Quickstart

1. Start the central backend: `cd apps/backend && bun run dev`
2. Launch the TUI: `cd apps/tui && bun run start`
3. Background sync daemon is initiated automatically upon valid authentication via `sync-daemon.ts`.